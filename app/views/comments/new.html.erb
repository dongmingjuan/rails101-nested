<%= form_for(@comment, url: comments_path(post_id: @post.id)) do |f| %>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, id: :comment_title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

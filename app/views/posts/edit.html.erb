<%= form_for(@post, url: post_path(group_id: @group.id), :method => :put) do |f| %>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, id: :post_title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
